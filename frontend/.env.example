# URL base de la API del alumno (domain-service).
# Cuando el alumno tenga su servicio levantado, crea un .env con este valor
# y el proxy de Vite apuntará al backend real en lugar del mock.
VITE_API_URL=http://localhost:8080
